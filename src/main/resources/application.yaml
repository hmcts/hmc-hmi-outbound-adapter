server:
  port: 4558

management:
  endpoint:
    health:
      show-details: "always"
  endpoints:
    web:
      base-path: /
      exposure:
        include: health, info, prometheus

#If you use a database then uncomment below lines and update db properties accordingly
spring:
  config:
    import: "optional:configtree:/mnt/secrets/hmc/"
  application:
    name: HMC HMI Outbound Adapter
  jackson:
    property-naming-strategy: SNAKE_CASE
  cloud:
    discovery:
      client:
        composite-indicator:
          enabled: false
#  datasource:
#    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}${DB_OPTIONS:}
#    username: ${DB_USER_NAME}
#    password: ${DB_PASSWORD}
#    properties:
#      charSet: UTF-8
#    hikari:
#      minimumIdle: 2
#      maximumPoolSize: 10
#      idleTimeout: 10000
#      poolName: {to-be-defined}HikariCP
#      maxLifetime: 7200000
#      connectionTimeout: 30000
#  jpa:
#    properties:
#      hibernate:
#        jdbc:
#          lob:
#            # silence the 'wall-of-text' - unnecessary exception throw about blob types
#            non_contextual_creation: true

azure:
  application-insights:
    instrumentation-key: ${hmc.AppInsightsInstrumentationKey:00000000-0000-0000-0000-000000000000}
  jms:
    servicebus:
      outbound-connection-string: ${HMC_SERVICE_BUS_OUTBOUND_CONNECTION_STRING}
      outbound-queue-name: ${HMC_SERVICE_BUS_OUTBOUND_QUEUE}
      # should be an integer that is interpreted as seconds
      exponential-multiplier: ${SERVICE_BUS_EXPONENTIAL_MULTIPLIER:2}
      max-retry-attempts: ${SERVICE_BUS_MAX_RETRIES:5}
      inbound-connection-string: ${HMC_SERVICE_BUS_INBOUND_CONNECTION_STRING}
      inbound-queue-name: ${HMC_SERVICE_BUS_INBOUND_QUEUE}

fh:
  ad:
    host: "https://login.microsoftonline.com/531ff96d-0ae9-462a-8d2d-bec7c0b42082/"
    get-token-url: "oauth2/v2.0/token"
    client-id: "a12e8358-fbd8-4a98-84aa-a106161dfbd7"
    client-secret: "tHp8Q~_uhkyZfjzpKRtebyC68SGFC.ACfGQdObPh"
    scope: "api://d49d7fa2-6904-4223-a907-3b8992af4190/.default"
    grant-type: "client_credentials"
  hmi:
    host: "https://hmi-apim.staging.platform.hmcts.net/hmi"
    source-system: "CFT"
    destination-system: "MOCK"

feign:
  okhttp:
    enabled: true

LOGGING:
  LEVEL:
    uk:
      gov:
        hmcts:
          reform:
            hmc: INFO
